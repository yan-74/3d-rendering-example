//STEP 2: Vertex shading

//To simulate the transformations done in vertex shading, we can rotate the cube in the animation loop!
//This animation is run every frame

cube.rotation.x += 0.01;
cube.rotation.y += 0.01;